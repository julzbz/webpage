{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch index.html (static file)",
            "type": "firefox",
            "request": "launch",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}
